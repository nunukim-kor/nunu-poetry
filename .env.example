# A long, random value used to sign administrator sessions.
AUTH_SECRET=replace-with-at-least-32-random-characters
# Password for /admin. Store a bcrypt hash in production if adapting authentication.
ADMIN_PASSWORD=replace-with-a-strong-password
