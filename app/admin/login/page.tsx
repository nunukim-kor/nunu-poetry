"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export default function LoginPage() { const router = useRouter(); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function login(event: FormEvent) { event.preventDefault(); setLoading(true); setError(""); const response = await fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ password }) }); setLoading(false); if (!response.ok) return setError((await response.json()).error); router.replace("/admin"); router.refresh(); } return <main className="page pt-28"><form onSubmit={login} className="max-w-sm"><h1 className="book-type mb-10 text-xl font-normal">관리</h1><label className="block text-xs" htmlFor="password">비밀번호</label><input id="password" type="password" autoComplete="current-password" value={password} onChange={(e) => setPassword(e.target.value)} className="mt-2 w-full border-b border-neutral-400 bg-transparent py-2 outline-none focus:border-black" required /><button disabled={loading} className="mt-7 underline underline-offset-4 disabled:text-neutral-400">{loading ? "확인 중" : "로그인"}</button>{error && <p className="mt-5 text-xs text-red-700">{error}</p>}</form></main>; }
