"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
type Visibility = "public" | "private";
type EditorPoem = { id?: string; title: string; date: string; body: string[]; visibility: Visibility };
const today = new Date().toISOString().slice(0, 10);
export default function PoemEditor({ poem }: { poem?: EditorPoem }) { const router = useRouter(); const [title, setTitle] = useState(poem?.title ?? ""); const [date, setDate] = useState(poem?.date ?? today); const [body, setBody] = useState(poem?.body.join("\n") ?? ""); const [visibility, setVisibility] = useState<Visibility>(poem?.visibility ?? "private"); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); async function save(event: FormEvent) { event.preventDefault(); setSaving(true); setError(""); const response = await fetch(poem?.id ? `/api/admin/poems/${poem.id}` : "/api/admin/poems", { method: poem?.id ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ title, date, body: body.split("\n"), visibility }) }); setSaving(false); if (!response.ok) return setError((await response.json()).error ?? "저장할 수 없습니다."); router.replace("/admin"); router.refresh(); } async function remove() { if (!poem?.id || !confirm("이 시를 삭제할까요?")) return; const response = await fetch(`/api/admin/poems/${poem.id}`, { method: "DELETE" }); if (!response.ok) return setError("삭제할 수 없습니다."); router.replace("/admin"); router.refresh(); } return <form onSubmit={save} className="max-w-[680px]"><label className="block text-xs" htmlFor="title">제목</label><input id="title" value={title} onChange={(e) => setTitle(e.target.value)} className="mt-2 w-full border-b border-neutral-300 bg-transparent py-2 text-lg outline-none focus:border-black" required maxLength={180} /><label className="mt-8 block text-xs" htmlFor="date">발표일</label><input id="date" type="date" value={date} onChange={(e) => setDate(e.target.value)} className="mt-2 border-b border-neutral-300 bg-transparent py-2 outline-none focus:border-black" required /><label className="mt-12 block text-xs" htmlFor="body">본문</label><textarea id="body" value={body} onChange={(e) => setBody(e.target.value)} className="book-type mt-3 min-h-[390px] w-full resize-y border-0 bg-transparent px-0 py-2 text-[18px] leading-[2] outline-none" required maxLength={20000} /><label className="mt-8 block text-xs" htmlFor="visibility">공개 상태</label><select id="visibility" value={visibility} onChange={(e) => setVisibility(e.target.value as Visibility)} className="mt-2 border-b border-neutral-300 bg-transparent py-2"><option value="public">공개</option><option value="private">비공개</option></select><div className="mt-10 flex items-center gap-7"><button disabled={saving} className="underline underline-offset-4 disabled:text-neutral-400">{saving ? "저장 중" : "저장"}</button>{poem?.id && <button type="button" onClick={remove} className="text-xs text-neutral-500 underline underline-offset-4">삭제</button>}</div>{error && <p className="mt-5 text-xs text-red-700">{error}</p>}</form>; }
